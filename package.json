{
  "name": "fortify",
  "productName": "Fortify",
  "version": "1.0.0",
  "description": "",
  "main": "out/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "npm run build:js && npm run build:package",
    "build:package": "electron-packager . --arch=x64 --out=build --icon=icon.icns --overwrite --electron-version=1.6.11",
    "build:js": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/node": "^7.0.28",
    "@types/winston": "^2.3.3",
    "ts-loader": "^2.1.0",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "node-webcrypto-ossl": "^1.0.28",
    "webcrypto-local": "git+ssh://git@github.com/PeculiarVentures/webcrypto-local.git",
    "winston": "^2.3.1"
  }
}
